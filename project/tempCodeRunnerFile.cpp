int answer = (num[i] == '*' ? stoi(ladd)*stoi(radd) : stoi(ladd)/stoi(radd));
                // num.erase(start,(end-start)+1);
                // for(int k = start ; k < num.length() ; k++)
                // {
                //     store += num[k];
                // }
                // num.erase(start,(num.length()-2));
                // cout<<end<<endl;
                // for(int b = start,k = 0 ; b < start+(to_string(answer).length()) ; b ++,k++)
                // {
                //     num += to_string(answer)[k];
                // }
                // num += store;