#include <iostream>
using namespace std;
int power(int x,int y)
{
    int pow = 1;
    if(x > 30 || y > )
    for(int i = 1;i<=y;i++)
    {
        pow *= x;
    }
    return pow;
}
int main(){
    cout<<power(20,10);
}